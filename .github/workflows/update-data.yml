# TEMPORARILY OVERRIDDEN ON THIS BRANCH ONLY: dispatching "Update card data" with
# ref=claude/cache-issues-acquisition-nx6ai9 runs this cache diagnostic instead of
# publishing data. main's copy is untouched; restore before merging.
name: Update card data
on:
  workflow_dispatch: {}
permissions: {}
concurrency:
  group: data-cache-test
jobs:
  publish-data:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v6
      - uses: actions/setup-node@v6
        with:
          node-version: "24"
      - name: CardTrader API cache probe (no publishing)
        run: node cloud/cache-test.js
        env:
          CARDTRADER_TOKEN: ${{ secrets.CARDTRADER_TOKEN }}
